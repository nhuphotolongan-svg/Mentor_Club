name = "{{SLUG}}"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"
# nodejs_compat: cần cho một số API Node; cloudflare:sockets (SMTP) hoạt động sẵn trên Pages Functions.
compatibility_flags = ["nodejs_compat"]

# Biến KHÔNG nhạy cảm để ở đây (commit được). Biến bí mật dùng `wrangler pages secret put`.
[vars]
BRAND_NAME = "{{TEN}}"
BRAND_COLOR = "{{MAU}}"
EMAIL_TEMPLATE_KEY = "x3_registration"
